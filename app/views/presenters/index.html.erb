<section class="container__presenter">
  <h1 class="w-100 bg-gray-400 text-center py-4 text-3xl text-white">Presenter's List</h1>
  <%= link_to "Add New Presenter", new_presenter_path, class: '' %>
  <ul>
    <% @presenters.each do |presenter|%>
    <li><%= link_to presenter.name, presenter%></li>
    <% end %>
  </ul>
</section>


